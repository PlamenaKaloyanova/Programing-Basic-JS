function projectMaking(input) {
    let arhitectName = input[0];
    let projectNumber = Number(input[1]);
    let oneProject = 3;
    let requiredHours = projectNumber * oneProject;
    console.log('The architect' + ' ' + arhitectName + ' ' + 'will need' + ' ' + requiredHours + ' ' + 'hours' + ' ' + 'to complete' + ' ' + projectNumber + ' ' + 'project/s.')
}
